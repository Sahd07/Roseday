<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Happy Rose Day 🌹</title>

<style>
*{margin:0;padding:0;box-sizing:border-box;font-family:'Segoe UI', sans-serif;}
body{
  height:100vh;
  background: linear-gradient(135deg,#ff9a9e,#fad0c4);
  overflow:hidden;
  display:flex;
  align-items:center;
  justify-content:center;
}
.card{
  background: rgba(255,255,255,0.95);
  padding:25px;
  border-radius:20px;
  width:90%;
  max-width:400px;
  text-align:center;
  box-shadow:0 15px 30px rgba(0,0,0,0.2);
  animation: fadeIn 2s ease;
}
.rose{font-size:45px;animation: beat 1.5s infinite;}
h1{color:#ff4d6d;margin:10px 0;}
h2{color:#ff6f91;min-height:28px;}
p{color:#444;font-size:14px;line-height:1.6;display:none;margin-top:15px;}
button{
  margin-top:15px;
  padding:10px 20px;
  background:#ff4d6d;
  color:white;
  border:none;
  border-radius:25px;
  font-size:15px;
  cursor:pointer;
}
button:hover{background:#ff2f5d;}
img{
  width:120px;
  height:120px;
  object-fit:cover;
  border-radius:50%;
  margin-top:10px;
  display:none;
  border:4px solid #ff6f91;
}
@keyframes beat{0%,100%{transform:scale(1);}50%{transform:scale(1.2);}}
@keyframes fadeIn{from{opacity:0;transform:translateY(20px);}to{opacity:1;transform:translateY(0);}}
.floating-rose{position:absolute;font-size:24px;animation: floatUp linear infinite;}
@keyframes floatUp{from{transform:translateY(100vh);opacity:1;}to{transform:translateY(-10vh);opacity:0;}}
</style>
</head>

<body>

<!-- Background music -->
<audio autoplay loop>
  <source src="https://files.catbox.moe/9z0j8b.mp3" type="audio/mpeg">
</audio>

<div class="card">
  <div class="rose">🌹</div>
  <h1>Happy Rose Day</h1>
  <h2 id="typing"></h2>

  <!-- Optional Photo -->
  <img src="https://i.imgur.com/8Km9tLL.jpg" id="photo">

  <button onclick="reveal()">Tap to open my heart 💖</button>

  <p id="msg">
    On this Rose Day, I want to give you this rose as a small symbol of my feelings.
    Just like a rose adds beauty and fragrance, you’ve added happiness and warmth to my life.
    Every time you smile, my day becomes brighter.
    <br><br>
    <b>Happy Rose Day 🌹💖</b>
  </p>
</div>

<script>
// Typing effect
const text = "Juuu babyyy 💖";
let i = 0;
function typing(){
  if(i < text.length){
    document.getElementById("typing").innerHTML += text.charAt(i);
    i++;
    setTimeout(typing,120);
  }
}
typing();

// Reveal message
function reveal(){
  document.getElementById("msg").style.display = "block";
  document.getElementById("photo").style.display = "block";
}

// Floating roses
function createRose(){
  const rose = document.createElement("div");
  rose.className = "floating-rose";
  rose.innerHTML = "🌹";
  rose.style.left = Math.random()*100 + "vw";
  rose.style.animationDuration = (3 + Math.random()*5) + "s";
  document.body.appendChild(rose);
  setTimeout(()=>{ rose.remove(); },8000);
}
setInterval(createRose,500);
</script>

</body>
</html>